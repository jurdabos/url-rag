# Extra Python packages beyond what the Astronomer runtime provides.
# Source of truth: pyproject.toml (managed by uv).
# Only direct dependencies are listed here; the runtime resolves transitive deps.
openai>=2.24.0
pinecone>=8.1.0
beautifulsoup4>=4.14.3
pendulum>=3.2.0
ijson>=3.3.0
